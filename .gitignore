# Dependencies
node_modules/

# Build output
dist/
tsconfig.tsbuildinfo

# Compiled JS artifacts alongside TS source files
# (tsc emits .js/.d.ts next to .ts when outDir is not set — e.g. the cli debug
# wrapper in bin/, which is run directly and excluded from the composite build)
packages/backend/apps/cli/bin/cli-debug.js
packages/backend/apps/cli/bin/cli-debug.d.ts
packages/backend/apps/cli/bin/cli-debug.d.ts.map
packages/frontend/apps/web/vite.config.js
packages/frontend/apps/web/vite.config.d.ts
packages/frontend/apps/web/vite.config.d.ts.map
*.js.map

# Environment
.env
.env.local
# Shared dev secrets — symlinked per worktree, distributed privately, never
# committed. The committed template is packages/backend/.env.development.example.
.env.development

# Data
data/pg/
data/blobs/
data/grafana/
data/seed.dump

# OS
.DS_Store


# --- AI Code Flow (managed by install.sh) ---
CLAUDE.md
.claude/agents
.claude/commands
.claude/skills/TypeScript
.claude/skills/blueprint
.claude/skills/datadog-pup
.claude/skills/engineering-project-manager
.claude/skills/GitFlow
.claude/skills/linearis-cli
.claude/skills/LinearMCP
.claude/skills/notion-cli
.claude/skills/TDD
.claude/skills/ticket-writer
templates
docs/context/git-conventions.md
docs/summaries/handoff-2026-03-30-C-17413-execute.md
# --- End AI Code Flow ---

# devctl worktree machinery (absolute-path symlinks, host-local)
.devctl_generated/

# husky internals — regenerated by `husky` on install. Husky ships its own
# .gitignore inside this dir, but the devctl symlink chain breaks it in
# worktrees, so ignore it explicitly here.
.husky/_/



.nx/cache
.nx/workspace-data
.nx/migrate-runs