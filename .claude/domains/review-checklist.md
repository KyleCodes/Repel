# Domain Review Checklist

Domain-specific review items that `code-reviewer` should check in addition to the
standard checklist. Each item should be a concrete, checkable concern.

<!-- Example:
- SIP: are state machine transitions correct?
- RTP hot path: any heap allocations per packet?
- Provider calls: timeouts, retry/backoff, graceful degradation in place?
- Any unsanitised input in SIP headers?
-->
