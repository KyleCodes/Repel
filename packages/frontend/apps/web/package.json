{
  "name": "@repel/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build && vite build",
    "dev": "vite",
    "preview": "vite preview",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "@repel/enums": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.4"
  },
  "nx": {
    "tags": [
      "type:app",
      "scope:frontend"
    ]
  },
  "_futureDependencies": {
    "//": "Parked here so @nx/dependency-checks (no unused deps) stays green while the SPA is still a stub. Move back into dependencies as soon as the app imports them.",
    "@tanstack/react-query": "^5.51.1",
    "@tanstack/react-router": "^1.45.3",
    "zustand": "^4.5.4"
  }
}
